$('#new_type_link').hide().after("<%= j render 'form' %>");

element_id_when_active_update_form = $('form.edit_type').attr('id').replace('edit', 'update');
$('#' + element_id_when_active_update_form + '').show();
$('form.edit_type').remove();