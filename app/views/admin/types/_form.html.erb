<%= form_for([:admin, @type]) do |f| %>
    <%= render 'shared/error', object: @type %>
    <div class='form-group'>
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control', placeholder: 'Input type of Ads' %>
    </div>
    <%= f.submit class:'btn btn-primary' %>
<% end %>